<div>
  <div>
    <p>ユーザーネーム：<%= @user.name %></p>
  </div>

  <div>
    <p>メールアドレス：<%= @user.email %></p>
  </div>

  <div>
    <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
  </div>

  <div>
    <p>登録情報　<%= link_to '編集する', edit_user_path(@user.id), class: 'btn btn-success' %></p>
  </div>

</div>


